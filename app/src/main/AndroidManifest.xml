<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projetoconsutoria">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/projeto_consultoria"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/projeto_consultoria"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ContatoActivity"></activity>
        <activity android:name=".ClienteActivity" />
        <activity android:name=".ServicoActivity" />
        <activity android:name=".EmpresarialActivity" />
        <activity android:name=".ConsultoriaTeste">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>